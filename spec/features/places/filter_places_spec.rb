describe "Filtering places" do
	it "shows lowest price" 
	it "shows highest price"
	it "shows largest meal"
	it "shows closest restaurant"
	 it "shows recent movies" 
  #   movie = Movie.create!(movie_attributes(released_on: 1.day.ago))

  #   visit movies_url

  #   click_link "Recent"

  #   expect(current_path).to eq("/movies/filter/recent")

  #   expect(page).to have_text(movie.title)
  # end
end