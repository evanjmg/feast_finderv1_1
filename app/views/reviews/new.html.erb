 <% content_for(:title, "Submit a Review") %>
<header>
	<h1> Write a Review for <%= link_to @place.name, @place %></h1>
</header>
	<%= render "shared/errors", object: @review %>
<%= render 'reviews/form' %>